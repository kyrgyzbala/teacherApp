<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.english.teacherapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/new_logo"
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/new_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:targetApi="n">
        <activity android:name=".ui.notes.ViewNoteActivity"></activity>
        <activity android:name=".ui.quizzes.QuestionsActivity" />
        <activity android:name=".ui.chat.PrivateChatActivity" />
        <activity android:name=".ui.videos.AddVideoActivity" />
        <activity android:name=".ui.videos.VideosActivity" />
        <activity android:name=".ui.quizzes.QuizzesActivity" />
        <activity android:name=".ui.notes.NotesActivity" />
        <activity android:name=".ui.home.types.TypesActivity" />
        <activity android:name=".ui.login.register.RegisterActivity" />
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.login.login.LoginActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>